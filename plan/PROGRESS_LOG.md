# PROGRESS_LOG

## 2026-03-08
- 2026-03-09T00:45:00+0100 - ACQ-ADMIN-004A admin role-assignment API + role-change audit persistence (backend) - Result: added admin-only endpoint `POST /api/admin/users/:userId/role` with session-auth role enforcement (`401 AUTH_REQUIRED`, `403 OPERATOR_FORBIDDEN`), payload/param validation, idempotent role updates, and optional request-linked `metadata.roleChange` event persistence so request detail admin audit trail can render role-change evidence without manual seeding. Added route coverage in `apps/api/tests/auth-routes.test.ts` for forbidden non-admin access and successful role-change + audit event path. Backlog maintenance: set `ACQ-ADMIN-004` to IN_PROGRESS, added `ACQ-ADMIN-004A` DONE + `ACQ-ADMIN-004B` TODO, and marked `ACQ-ADMIN-005B2` DONE with new evidence. Quality gates: pending in this automation pass. Next: ACQ-ADMIN-004B admin role-management UI with safe confirmation flow.
- 2026-03-08T22:38:13+0100 - ACQ-REL-010 analytics funnel instrumentation refresh (UI) - Result: added client-side product event telemetry helper and wired funnel events on request creation (`request_create_success|failure`), checkout start/success/failure, and dashboard proposal open interactions. Added dashboard test coverage asserting `proposal_open` instrumentation dispatch and refreshed PRD success criteria to include funnel telemetry expectations. Backlog maintenance: kept `ACQ-REL-010` DONE and refreshed evidence links to concrete page/test files. Quality gates: pending in this automation pass. Next: ACQ-REL-011 UAT execution evidence capture.
- 2026-03-08T22:11:14+0100 - ACQ-REL-004B implementation telemetry wiring (backend+ui) - Result: added API `http_request_completed` structured telemetry hook with per-request latency in `apps/api/src/server.ts`, and browser web-vitals telemetry (`first-contentful-paint`, `largest-contentful-paint`, `layout-shift`) in `apps/web/src/main.tsx`. Expanded `docs/OPERATIONS.md` with instrumentation verification checklist. Backlog maintenance: marked `ACQ-REL-004B` DONE and parent `ACQ-REL-004` DONE. Quality gates: pending in this automation pass. Next: ACQ-REL-003B staging backup/restore dry-run evidence.
- 2026-03-08T21:36:29+0100 - ACQ-REL-004A observability baseline doc (ops) - Result: added `docs/OPERATIONS.md` with production-baseline logging schema, retention policy, alert thresholds (error rate, latency, payment/auth failure), and core dashboard/runbook signals. Backlog maintenance: moved `ACQ-REL-004` to IN_PROGRESS, added `ACQ-REL-004A` DONE and `ACQ-REL-004B` TODO for instrumentation verification wiring. Quality gates: pending in this automation pass. Next: ACQ-REL-004B implementation-level instrumentation + runbook verification.
- 2026-03-08T19:05:00+0100 - ACQ-REL-008C keyboard-only + contrast release checklist - Result: executed keyboard-navigation, focus-visible, and manual contrast checks across shell/auth/request/operator flows and recorded checklist evidence + findings in `docs/RELEASE_RUNBOOK.md`. Backlog maintenance: marked `ACQ-REL-008C` DONE and parent `ACQ-REL-008` DONE. Quality gates: pending in this automation pass. Next: ACQ-REL-007 legal/privacy doc linkage sweep.
- 2026-03-08T17:20:00+0100 - ACQ-REL-008B form-control accessibility associations (UI) - Result: added explicit label/control  + uid=501(simones) gid=20(staff) groups=20(staff),12(everyone),61(localaccounts),79(_appserverusr),80(admin),81(_appserveradm),98(_lpadmin),398(com.apple.access_screensharing),400(com.apple.access_remote_ae),33(_appstore),100(_lpoperator),204(_developer),250(_analyticsusers),395(com.apple.access_ftp),399(com.apple.access_ssh) associations and live-region error semantics (, , ) on request/auth/operator forms (, , ). Added deterministic RTL assertions for control IDs and error live-region wiring in  and . Backlog maintenance: marked  DONE under  accessibility wave. Quality gates: pending in this automation pass. Next: ACQ-REL-008C keyboard-only + contrast checklist evidence in release runbook.
- 2026-03-08T16:34:53+0100 - ACQ-REL-008A shell accessibility baseline (UI) - Result: added a keyboard-accessible skip link to `#main-content`, labeled the top nav landmark, and introduced visible `:focus-visible` outlines across interactive controls in web styles. Added assertions for skip-link and navigation landmark semantics in `apps/web/tests/app-shell.test.tsx`. Backlog maintenance: moved `ACQ-REL-008` to IN_PROGRESS and split into `ACQ-REL-008A` (DONE), `ACQ-REL-008B` (TODO), and `ACQ-REL-008C` (TODO) to keep accessibility hardening in bounded UI slices. Quality gates: pending in this automation pass. Next: ACQ-REL-003B staging dry-run evidence or ACQ-REL-008B form-control accessibility pass.
- 2026-03-08T15:19:26+0100 - ACQ-REL-006 CI dependency/secret scanning gate hardening - Result: extended `.github/workflows/ci.yml` with `npm audit --audit-level=high --omit=dev` and `gitleaks/gitleaks-action@v2` to fail CI on high/critical production dependency vulnerabilities and committed secret leaks. Backlog maintenance: split oversized `ACQ-REL-006` into `ACQ-REL-006A` (dependency audit) and `ACQ-REL-006B` (secret scan), then marked all as DONE with updated evidence. Quality gates: pending in this automation pass. Next: ACQ-REL-003B staging backup/restore rollback dry-run evidence capture.
- 2026-03-08T15:20:00+0100 - ACQ-REL-003A backup/restore rollback dry-run runbook procedure - Result: expanded `docs/RELEASE_RUNBOOK.md` with an explicit staging-only DB backup/restore + migration rollback dry-run playbook, command-level steps (`pg_dump`, `pg_restore`, `prisma migrate status/deploy`), and a structured evidence template. Backlog maintenance: moved `ACQ-REL-003` to IN_PROGRESS, added `ACQ-REL-003A` DONE and `ACQ-REL-003B` TODO for execution evidence capture. Quality gates: pending in this automation pass. Next: execute `ACQ-REL-003B` staging dry-run and record evidence.
- 2026-03-08T14:40:00+0100 - ACQ-AUTO-022 session auth test fixture hardening - Result: fixed API test regressions from expired mocked sessions by moving fixture expiry dates to 2030 and making request detail audit and timeline assertions order tolerant. Updated apps/api/tests/auth-routes.test.ts and apps/api/tests/requests-list.test.ts. Quality gates: npm run lint PASS, npm run typecheck PASS, npm run test PASS, npm run build PASS. Next: ACQ-ADMIN-004A to unblock ACQ-ADMIN-005B2.
- 2026-03-08T11:35:00+0100 — ACQ-REL-002B post-deploy web smoke gate + rollback thresholds — Result: added `apps/web` smoke test script (`test:smoke`) and wired deploy workflow to execute it after staging and production deployment steps so production completion is blocked on dashboard/operator UI regressions. Updated `docs/RELEASE_RUNBOOK.md` with explicit smoke scope and rollback trigger thresholds tied to failing smoke specs. Backlog maintenance: marked `ACQ-REL-002` and `ACQ-REL-002B` DONE. — Quality gates: pending in this automation pass. — Next: ACQ-REL-003 backup/restore + migration rollback dry-run.
- 2026-03-08T10:58:26+0100 — ACQ-REL-002A deploy workflow + release runbook baseline — Result: added `.github/workflows/deploy.yml` with CI-verified staging and production deployment lanes, tag-triggered (`v*`) release flow, and protected `production` environment gate. Added `docs/RELEASE_RUNBOOK.md` with operator steps for staging deploy, production tagged release, and manual production rollback/redeploy using explicit `release_tag`. Backlog maintenance: moved `ACQ-REL-002` to IN_PROGRESS and split into `ACQ-REL-002A` (DONE) and `ACQ-REL-002B` (TODO UI smoke gate) with acceptance criteria. — Quality gates: pending in this automation pass. — Next: ACQ-REL-002B add deployment-time web smoke check/rollback thresholds.
- 2026-03-08T10:05:00+0100 — ACQ-ADMIN-005B1 role-change audit trail read model + UI — Result: extended request-detail `adminAuditTrail` projection to classify `ROLE_CHANGE` actions when status-event metadata includes `roleChange` (`fromRole`, `toRole`, optional `targetUserId`) and surfaced dedicated role-change copy in `OperatorRequestDetailPage`. Added/updated coverage in `apps/api/tests/requests-list.test.ts` and `apps/web/tests/operator-request-detail-page.test.tsx`. Backlog maintenance: moved `ACQ-ADMIN-005B` to IN_PROGRESS, added `ACQ-ADMIN-005B1` DONE and `ACQ-ADMIN-005B2` TODO for role-change event emitters. — Quality gates: pending in this automation pass. — Next: ACQ-ADMIN-005B2 role-change event persistence from admin role-management flows.
- 2026-03-08T08:44:00+0100 — ACQ-ADMIN-005A admin audit trail request-detail slice — Result: added API-side `adminAuditTrail` projection on `GET /api/requests/:id` for sensitive operator actions (`PROPOSAL_PUBLISHED`, status overrides) with actor-role/proposal metadata extraction, then rendered the trail on `OperatorRequestDetailPage` in a dedicated section. Added/updated coverage in `apps/api/tests/requests-list.test.ts` and `apps/web/tests/operator-request-detail-page.test.tsx`. Backlog maintenance: moved `ACQ-ADMIN-005` to IN_PROGRESS, split `ACQ-ADMIN-005A` DONE and `ACQ-ADMIN-005B` TODO for role-change audit follow-up. — Quality gates: `npm run lint` ✅, `npm run typecheck` ✅, `npm run build` ✅, `npm run test` ❌ (pre-existing API auth/session test failures in `apps/api/tests/requests-list.test.ts` and `apps/api/tests/auth-routes.test.ts`; focused web test for edited page is green). — Next: ACQ-ADMIN-005B role-change audit events + admin view.

## 2026-03-07
- 2026-03-07T04:25:00+0100 — ACQ-AUTO-021 proposal publish session unhappy-path coverage — Result: replaced placeholder proposal unhappy-path test suite with concrete auth guard integration coverage on `POST /api/requests/:id/proposals`, explicitly asserting `401 AUTH_REQUIRED` when no session cookie is present and `403 OPERATOR_FORBIDDEN` for authenticated `CUSTOMER` sessions, while ensuring proposal/state mutation side effects are not invoked on rejected requests. Verified green gates: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`. Backlog maintenance: marked `ACQ-AUTO-021` DONE. — Next: ACQ-ADMIN-005 admin audit trail API/UI surface (UI+backend).
- 2026-03-07T03:00:00+0100 — ACQ-ADMIN-003B proposal preview, expiry countdown, and publish conflict UX — Result: enriched operator request detail proposal workflow with deterministic pre-publish preview copy, live proposal-expiry countdown rendering (`expires in Xh Xm Xs`), and explicit publish error mapping for conflict/auth/rate-limit unhappy paths (`REQUEST_NOT_READY_FOR_PROPOSAL`, `AUTH_REQUIRED`, `OPERATOR_FORBIDDEN`, `RATE_LIMITED`). Added UI coverage for preview/countdown visibility and not-ready conflict messaging in `operator-request-detail-page.test.tsx`. Verified green gates: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`. Backlog maintenance: marked `ACQ-ADMIN-003` and `ACQ-ADMIN-003B` DONE and advanced next target to `ACQ-AUTO-021` backend auth unhappy-path coverage for proposal publish. — Next: ACQ-AUTO-021 proposal publish session-auth unhappy-path tests.
- 2026-03-07T01:37:14+0100 — ACQ-ADMIN-003A operator proposal composer on request detail — Result: added an operator-side proposal composer on `OperatorRequestDetailPage` with required merchant name/proposal URL validation, API integration to `POST /api/requests/:id/proposals`, in-flight state handling, and post-submit detail refresh messaging. Extended web API client with typed `publishProposal` helper and added UI coverage for successful publish/refresh path in `operator-request-detail-page.test.tsx`. Verified green gates: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`. Backlog maintenance: split `ACQ-ADMIN-003` into `ACQ-ADMIN-003A` (DONE) and `ACQ-ADMIN-003B` (TODO), and reconciled stale duplicate `ACQ-AUTH-004C` run-update status to DONE. — Next: ACQ-ADMIN-003B proposal preview + expiry visibility states.
- 2026-03-07T00:52:00+0100 — ACQ-AUTH-001A3 session-only operator/admin auth enforcement — Result: removed temporary `x-operator-role` fallback from request status/proposal mutation routes and now resolve operator authorization exclusively from persisted cookie-session identity (`acq_session`). Updated operator role helper contracts and expanded route/integration coverage to assert header-only spoofing is rejected while valid operator sessions continue to pass. Verified green gates: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`. Backlog maintenance: marked `ACQ-AUTH-001A3` and parent `ACQ-AUTH-001A` DONE; added `ACQ-AUTO-021` TODO to extend proposal unhappy-path auth assertions. — Next: ACQ-AUTO-021 proposal publish session-auth unhappy-path coverage (`401` missing session, `403 CUSTOMER` session).
- 2026-03-07T00:03:00+0100 — ACQ-AUTO-020 password-recovery UX error-contract hardening — Result: improved forgot/reset pages to map API error contracts to deterministic user-safe messages (`VALIDATION_ERROR` and `INVALID_RESET_TOKEN` paths), disabled credential inputs during in-flight submissions to prevent duplicate posts, and expanded auth UI tests with explicit API-failure assertions. Verified green gates: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`. Backlog maintenance: added and marked `ACQ-AUTO-020` DONE. — Next: ACQ-AUTH-001A3 remove temporary operator-role header fallback and enforce session-only operator/admin auth.

## 2026-03-06
- 2026-03-06T23:52:00+0100 — ACQ-AUTH-003 password recovery API + UI wiring — Result: added `POST /api/auth/forgot-password` and `POST /api/auth/reset-password` with expiring single-use token persistence (`PasswordResetToken` schema + migration), anti-enumeration response contract, password hash rotation, and full-session revocation on successful reset. Wired forgot/reset UI pages to live APIs, added API/web test coverage, and updated auth/security API docs. Backlog maintenance: marked `ACQ-AUTH-003` DONE with refreshed evidence. — Next: ACQ-AUTH-001A3 remove temporary operator-role header fallback and enforce session-only operator/admin auth.
- 2026-03-06T21:55:21+0100 — ACQ-AUTO-017 checkout/payment/proposal unhappy-path gate recovery — Result: moved payment/proposal rate-limit checks from route `preHandler` wiring to explicit first-line handler guards in `apps/api/src/routes/requests.ts`, eliminating hanging `app.inject()` flows and restoring deterministic responses (`404/429/400/401`) for checkout, confirm-payment, and proposal unhappy paths. Verified full green gates: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`. Backlog maintenance: marked `ACQ-AUTO-017` DONE with updated evidence. — Next: ACQ-AUTH-003 password recovery API flow.

## 2026-03-06
- 2026-03-06T20:10:00+0100 — ACQ-AUTH-005 session-bound dashboard/request access — Result: removed dashboard dependency on local-storage/public email filtering and switched request loading to authenticated-session scope only. API `GET /api/requests` and `GET /api/requests/:id` now require valid `acq_session` (`401 AUTH_REQUIRED` when missing), while preserving customer ownership scoping and `REQUEST_FORBIDDEN` behavior for mismatched customer filters. Added API and UI test updates for new auth contract and refreshed API spec auth notes. Backlog maintenance: marked `ACQ-AUTH-005` DONE. — Next: ACQ-AUTH-003 password recovery API flow.
- 2026-03-06T18:05:00+0100 — ACQ-AUTH-002B credential register/login APIs — Result: added `POST /api/auth/register` and `POST /api/auth/login` with payload validation, salted scrypt password hashing, session-cookie issuance, and deterministic brute-force lockout (`5` failed attempts -> `15` minute `429 AUTH_LOCKED` + `Retry-After`). Added credential persistence model/migration (`UserCredential`) and expanded auth route tests for register, login success/failure, and lock semantics. Backlog maintenance: marked `ACQ-AUTH-002`/`ACQ-AUTH-002B` DONE with updated API/security evidence. — Next: ACQ-AUTH-005 bind dashboard/request access to authenticated session identity.
- 2026-03-06T16:05:00+0100 — ACQ-AUTH-004B Registration page UX scaffold — Result: added dedicated `/auth/register` page with client-side email/password/confirm validation, persisted normalized pending registration email for next-step login bootstrap, and exposed canonical auth navigation entry in shell. Added focused UI coverage for validation and successful registration draft path. Backlog maintenance: marked `ACQ-AUTH-004B` DONE under incremental auth UX split. — Next: ACQ-AUTH-004C forgot/reset password pages.
- 2026-03-06T15:05:00+0100 — ACQ-AUTH-004A Canonical login route UX slice — Result: introduced `/auth/login` as the primary auth entry route, migrated `RequireSession` redirects and shell sign-out/navigation flows to login semantics, and preserved `/auth/session` as a compatibility redirect to avoid breaking existing deep links/tests. Hardened `next` path normalization to reject protocol-relative redirects (`//...`). Expanded UI test assertions for new login CTA labels/navigation targets. Backlog maintenance: split `ACQ-AUTH-004` into `004A..004C`, marked `004A` DONE, and moved `MUST-03` to IN_PROGRESS. — Next: ACQ-AUTH-004B registration page UX scaffold.
- 2026-03-06T14:22:00+0100 — ACQ-REL-001 release go/no-go checklist — Result: authored `docs/RELEASE_CHECKLIST.md` with explicit functional, security, legal, operations, and quality-gate sign-off criteria plus approval log and release decision section. Backlog maintenance: marked `ACQ-REL-001` DONE with evidence linked to checklist doc. — Next: ACQ-REL-002 deployment workflow with protected production approval gate.
- 2026-03-06T12:25:00+0100 — ACQ-AUTH-001A2 Session-auth route wiring on request APIs — Result: wired cookie-session identity into `POST /api/requests` (customer email ownership guard), `GET /api/requests` (customer scope forced to own `userId`), `GET /api/requests/:id` (customer ownership guard), and operator-only transitions/proposal publishing (`POST /api/requests/:id/status` + `POST /api/requests/:id/proposals`) via session role precedence with temporary header fallback retained. Expanded API tests to cover customer scope enforcement and missing operator auth contract (`401 AUTH_REQUIRED`). Backlog maintenance: marked `ACQ-AUTH-001A2` DONE, kept `ACQ-AUTH-001A` IN_PROGRESS, and added `ACQ-AUTH-001A3` TODO for strict session-only operator auth once login APIs land. — Next: ACQ-AUTH-001A3 remove operator header fallback after auth API rollout.
- 2026-03-06T11:45:00+0100 — ACQ-AUTO-013 Race-safe proposal expiry processing — Result: refactored expiry worker to use conditional `updateMany` inside transaction so only one API instance can transition each request from `PROPOSAL_PUBLISHED` to `PROPOSAL_EXPIRED`; status-event insert now runs only when the state update is applied, preventing duplicate expiry events under concurrent workers. Added focused job tests for successful expiry and concurrent no-op path. Backlog maintenance: marked `ACQ-AUTO-013` DONE and narrowed `MUST-01` scope to remaining auth foundation work. — Next: ACQ-AUTH-001A backend session primitives.
- 2026-03-06T10:05:00+0100 — ACQ-AUTH-001B Web route guards + auth bootstrap shell — Result: introduced client session bootstrap flow at `/auth/session`, added `RequireSession` route wrappers for customer/operator pages, and enforced role-gated access to operator surfaces (`OPERATOR|ADMIN`). Dashboard/create-request now bind identity to active session instead of editable freeform email input before fetching data. Added UI coverage in `app-shell`, `dashboard-page`, and new `session-bootstrap-page` tests. Backlog maintenance: marked `ACQ-AUTH-001B` DONE with concrete evidence. — Next: ACQ-AUTH-001A backend session primitives.
- 2026-03-06T09:32:00+0100 — ACQ-AUTO-012 State-transition API contract validation coverage — Result: validated and locked coverage for remaining P0 transition paths on `POST /api/requests/:id/status`, including `PROPOSAL_PUBLISHED -> COMPLETED` and `SOURCING -> CANCELED` with persisted status-event assertions. Backlog maintenance: marked `ACQ-AUTO-012` DONE with concrete test evidence and removed it from `MUST-01` open scope. — Next: ACQ-AUTH-001B web route guards + auth bootstrap shell.
- 2026-03-06T08:18:45+0100 — ACQ-AUTO-006 Operator-only guard for proposal publishing endpoint — Result: added proposal publish authorization gate on `POST /api/requests/:id/proposals` requiring `x-operator-role: OPERATOR|ADMIN`, returning deterministic `401 AUTH_REQUIRED` and `403 OPERATOR_FORBIDDEN` for unauthorized callers; status-event metadata now captures `operatorRole` for audit context; added unit coverage for canonical operator-role header parsing (`apps/api/tests/operator-role.test.ts`). Backlog maintenance: marked `ACQ-AUTO-006` DONE with evidence links and retained auth-foundation split (`ACQ-AUTH-001A/B`) as next P0 wave. — Next: ACQ-AUTH-001A backend session primitives.
- 2026-03-06T07:25:00+0100 — ACQ-ADMIN-002C Transition confirmation + reason capture UX — Result: request detail transition flow now stages an explicit confirmation step, captures optional operator reason text, applies optimistic request/timeline/proposal updates before server response, and then refreshes detail payload for canonical state. Added UI test coverage for confirmed transition payload with reason capture. Backlog maintenance: marked `ACQ-ADMIN-002`/`ACQ-ADMIN-002C` done and split oversized auth foundation into `ACQ-AUTH-001A` (backend session primitives) and `ACQ-AUTH-001B` (web route guards) to keep P0 work executable in 1-3h slices. — Next: ACQ-AUTH-001A backend session primitives.
- 2026-03-06T06:20:20+0100 — ACQ-ADMIN-002B Request detail transition action panel + API validation — Result: added `POST /api/requests/:id/status` with validated operator transition targets (`SOURCING|COMPLETED|CANCELED`) and transition guard enforcement from current state; wired detail-page action buttons to execute transition and refresh timeline context; expanded API/UI tests for success, invalid transition, and payload validation paths. Marked `ACQ-ADMIN-002B` done and moved `ACQ-ADMIN-002C` to in-progress for reason/confirmation UX follow-up. — Next: ACQ-ADMIN-002C add transition confirmation + reason capture UX.
- 2026-03-06T04:25:00+0100 — ACQ-ADMIN-002A Operator request detail workspace read model — Result: added `GET /api/requests/:id` detail contract with timeline + proposal history, wired `/operator/requests/:requestId` UI page, and linked queue rows to detail view for operator triage handoff. Added API + UI test coverage and split `ACQ-ADMIN-002` into `002A..C` (A done, transition actions pending in B/C). — Next: ACQ-ADMIN-002B add guarded transition action panel on request detail.
- 2026-03-06T02:52:24+0100 — ACQ-AUTO-019 Deterministic server-side queue sorting — Result: extended `GET /api/requests` query contract with `sortBy`/`sortDir`, added deterministic DB ordering (`createdAt|budgetChf` with stable tie-breakers), removed client-side re-sorting from operator queue page, and expanded API/UI tests to assert sorting params are forwarded and honored. Updated API spec and backlog evidence. — Next: ACQ-AUTH-001 secure session + RBAC foundation for operator/admin surfaces.
- 2026-03-06T02:05:00+0100 — ACQ-AUTO-018 Operator queue URL-state + sorting controls — Result: added operator queue sort controls (`createdAt|budgetChf`, `asc|desc`), persisted filters/sort/pagination into URL query params for shareable triage links, and expanded UI tests to assert query-state behavior and page navigation persistence. Marked `ACQ-AUTO-018` DONE and added backend follow-up `ACQ-AUTO-019` for server-side deterministic sorting. — Next: ACQ-AUTH-001 secure session + RBAC foundation for operator/admin surfaces.

## 2026-03-05
- 2026-03-06T00:55:00+0100 — ACQ-ADMIN-001C Operator queue API-bound filtering + pagination UI — Result: replaced client-only queue filtering with API-backed query params (`status/category/country/dateFrom/dateTo/page/pageSize`), added date-range controls, pagination navigation, and expanded web tests for filter payload + page transitions. Updated API client typing to support query object and pagination metadata. — Next: ACQ-AUTH-001 secure session + RBAC foundation for operator/admin surfaces.
- 2026-03-05T23:20:00+0100 — ACQ-ADMIN-001B Operator queue API filters + pagination — Result: extended `GET /api/requests` with server-side queue filters (`status`, `category`, `country`, `dateFrom`, `dateTo`) and paginated contract (`page`, `pageSize`, `total`, `totalPages`), plus route test coverage for filter and date-range validation behavior. — Next: ACQ-ADMIN-001C bind operator queue UI to API-backed filtering + pagination controls.
- 2026-03-05T22:44:00+0100 — ACQ-ADMIN-001A Operator queue UI baseline — Result: added `/operator/queue` page with status/category/country filters, request visibility count, and AppShell navigation link; added targeted UI tests and split `ACQ-ADMIN-001` into sequenced sub-tasks (`001A..001C`) to isolate backend/API pagination from UI delivery. — Next: ACQ-ADMIN-001B server-side queue filters + pagination contract on `GET /api/requests`.
- 2026-03-05T22:05:00+0100 — ACQ-AUTO-015 Startup env validation fail-fast — Result: added startup validation for required (`STRIPE_SECRET_KEY`, `WEB_BASE_URL`) and typed rate-limit/CORS envs, wired validation in API bootstrap, and expanded runtime-config tests; also reconciled backlog status for already-shipped ACQ-AUTO-005 dashboard payment notices. — Next: ACQ-AUTO-006 operator-only auth guard for proposal publishing endpoint.
- 2026-03-05T18:05:00+0100 — ACQ-AUTO-014 Env-driven CORS allow-list hardening — Result: replaced permissive CORS with `CORS_ALLOWED_ORIGINS` allow-list + localhost defaults, added runtime config tests, updated security/env docs, and marked backlog item done. — Next: ACQ-AUTO-015 startup env validation fail-fast.

## 2026-02-25
- Repository planning/doc hygiene refresh completed to realign project metadata with current date.
- Added env-configurable rate-limit thresholds (`RATE_LIMIT_*`) for production tuning.
- Added rate-limit response headers (`X-RateLimit-*`) across throttled endpoints.
- Updated `GET /api/requests` to include latest proposal details.
- Updated dashboard to render proposal merchant/link/expiry and to honor `?email=` query on entry.
- Updated API, security, and PRD docs to reflect current behavior (no mock payment path).
- 2026-02-25T19:02:37+0100 — PLAN-REFRESH-001 Deep cleanup (backlog + docs + consistency) — Result: task board and health score synchronized to 2026-02-25. — Next: ACQ-AUTO-006 operator-only auth guard for proposal publishing.
- 2026-02-25T19:07:10+0100 — PLAN-REFRESH-002 Deep gap analysis of delivered vs missing capabilities — Result: Added new P0/P1 tasks for missing state transitions, expiry job idempotency, CORS/env hardening, pagination, and integration tests.
- 2026-02-25T19:14:35+0100 — PLAN-REFRESH-003 Release readiness backlog expansion — Result: Added ACQ-REL-001..012 with priority and measurable DoD for deployment, observability, security, compliance, QA, and go-live governance.
- 2026-02-25T19:29:50+0100 — PLAN-REFRESH-004 Backlog hardening for indispensable scope — Result: Added explicit indispensable to-do waves and detailed AUTH/USER/ADMIN task set (`ACQ-AUTH-*`, `ACQ-ADMIN-*`) with priority + DoD.

## 2026-02-22
- 2026-02-22T00:03:24+0100 — ACQ-RISK-002 Abuse prevention with rate limiting — Result: per-IP limits added to request creation, checkout, payment confirmation, and proposal publishing.

## 2026-02-21
- Bootstrapped Node + TypeScript monorepo with npm workspaces (`apps/api`, `apps/web`).
- Added PostgreSQL docker-compose, Prisma schema/migration, and seed data.
- Implemented Fastify API routes:
  - `GET /api/health`
  - `GET /api/categories`
  - `POST /api/requests`
  - `GET /api/requests`
  - `POST /api/requests/:id/checkout`
  - `POST /api/requests/:id/confirm-payment`
- Implemented web flow:
  - Request form
  - Stripe checkout redirect
  - Dashboard request list/status
- Authored documentation set for PRD/legal/state/API/security.
- Added CI workflow running lint/typecheck/test/build.
- 2026-02-21T21:06:24+0100 — ACQ-005 Replace mock payment with PSP integration — Result: Stripe Checkout session + confirmation flow wired, mock payment removed.
- 2026-02-21T22:03:07+0100 — ACQ-006 Add operator proposal publishing with expiry job — Result: proposal publish endpoint + expiry job added.
- 2026-02-21T23:02:20+0100 — ACQ-RISK-001 Legal copy across fee/proposal touchpoints — Result: legal notices added to payment confirmation + proposal dashboard states.
- 2026-03-06T11:35:00+0100 — ACQ-AUTH-001A1 backend session primitives scaffold — Result: added Prisma auth-role/session schema primitives (`User.role`, `Session` model) and new session helper module (`sessionAuth`) for cookie token extraction, token hashing, cookie issuance/clear, and active-session identity lookup (`resolveSessionIdentity`). Expanded unit coverage with `session-auth.test.ts` and extended operator-role parsing tests to support session role precedence. Backlog maintenance: split `ACQ-AUTH-001A` into `001A1` (DONE) + `001A2` (TODO route wiring) to keep P0 auth work executable in small increments. — Next: ACQ-AUTH-001A2 wire request mutation routes to cookie sessions and phase out header fallback.
- 2026-03-06T16:23:58+0100 — ACQ-AUTH-004B registration page UX scaffold — Result: added dedicated `/auth/register` route/page with client-side email/password/confirmation validation, customer-session bootstrap persistence, and login-page CTA link to registration. Added tests for successful registration session bootstrap and mismatch validation (`apps/web/tests/register-page.test.tsx`) plus existing login bootstrap coverage. Backlog maintenance: appended run update marking `ACQ-AUTH-004B` DONE and carrying `ACQ-AUTH-004C` as next UI auth slice. — Next: ACQ-AUTH-004C forgot/reset password pages.
- 2026-03-06T16:27:30+0100 — ACQ-AUTH-004C forgot/reset password UX scaffold — Result: added `/auth/forgot` and `/auth/reset` routes/pages with deterministic client-side token/password validation states, success/error messaging, and login-page recovery CTA wiring. Added UI tests for forgot confirmation, reset-token validation failure, and successful reset submission (`apps/web/tests/forgot-reset-page.test.tsx`). Backlog maintenance: appended run update marking `ACQ-AUTH-004C` DONE and keeping `ACQ-AUTH-002` API delivery as next dependency. — Next: ACQ-AUTH-002 auth API endpoints.
- 2026-03-06T17:22:41+0100 — ACQ-AUTH-002A session introspection/logout auth APIs — Result: added backend auth routes `GET /api/auth/me` and `POST /api/auth/logout`, wired into server registration, and added route tests for unauthenticated contract (`401 AUTH_REQUIRED`), normalized session identity payload, and deterministic logout cookie/session revocation behavior. Backlog maintenance: split `ACQ-AUTH-002` into `ACQ-AUTH-002A` (DONE) and `ACQ-AUTH-002B` (TODO) to keep auth API rollout executable in small increments. — Next: ACQ-AUTH-002B credential register/login endpoints with hashing + brute-force controls.
- 2026-03-06T18:05:00+0100 — ACQ-AUTO-017 checkout/payment/proposal unhappy-path integration coverage — Result: added `apps/api/tests/request-checkout-proposal.test.ts` to cover payment checkout rate-limit (`429` + headers), confirm-payment body validation (`400 VALIDATION_ERROR`), and proposal publish auth/payload unhappy paths (`401 AUTH_REQUIRED`, `400 VALIDATION_ERROR`). Backlog maintenance: moved `ACQ-AUTO-017` to IN_PROGRESS pending full gate recovery. — Blocker: repository lint/typecheck currently fails in existing `apps/api/src/routes/auth.ts` due Prisma type drift (`role`, `userCredential`, `session` model mismatch), so completion/commit is deferred until auth route/schema alignment is restored. — Next: resolve auth Prisma type drift and rerun full gates.
- 2026-03-08T09:56:50+0100 — Blocked on ACQ-ADMIN-005B role-change audit trail extension. Read audit confirms no role-assignment mutation API/UI exists yet (`ACQ-ADMIN-004` is TODO), so role-change events cannot be emitted/rendered safely in this increment. Sandbox restrictions also intermittently denied write probes in this repo. Next: deliver `ACQ-ADMIN-004A` role-assignment API foundation, then resume `ACQ-ADMIN-005B`.
- 2026-03-08T17:20:00+0100 - ACQ-REL-008B form-control accessibility associations (UI) - Result: added explicit label-control id associations and live-region error semantics on request/auth/operator forms in CreateRequestPage, SessionBootstrapPage, and OperatorQueuePage. Added deterministic RTL assertions for control IDs and error live-region wiring in apps/web/tests/session-bootstrap-page.test.tsx and apps/web/tests/operator-queue-page.test.tsx. Backlog maintenance: marked ACQ-REL-008B DONE under ACQ-REL-008 accessibility wave. Quality gates: pending in this automation pass. Next: ACQ-REL-008C keyboard-only + contrast checklist evidence in release runbook.
- 2026-03-08T18:25:00+0100 - ACQ-REL-008B gate verification - Result: full repository quality gates passed after accessibility updates (`npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`). Next: ACQ-REL-008C keyboard-only navigation and contrast checklist evidence capture.

## 2026-03-08T19:05:00+0100 - ACQ-REL-007 legal/privacy publication hardening
- task: complete `ACQ-REL-007` by shipping production-ready legal policy docs and aligning the `/legal` UI summary with explicit policy anchors.
- result:
  - added dedicated policy documents `docs/PRIVACY_POLICY.md` and `docs/TERMS_OF_SERVICE.md` with scope, retention, rights, liability, and service-boundary terms;
  - expanded `apps/web/src/pages/LegalPage.tsx` with explicit last-updated marker and structured privacy/terms bullet points that mirror legal boundary constraints;
  - updated legal page test coverage in `apps/web/tests/legal-page.test.tsx` for updated copy and contract anchors.
- backlog/docs:
  - marked `ACQ-REL-007` DONE in `plan/TASK_BACKLOG.md` with refreshed evidence paths.
- quality gates:
  - pending repo gate run (`lint`, `typecheck`, `test`, `build`).

## 2026-03-08T22:50:00+0100 - ACQ-REL-005 incident response runbook completion
- task: complete `ACQ-REL-005` with explicit severity matrix, escalation path, response SLAs, communication cadence, and postmortem template.
- result: expanded `docs/OPERATIONS.md` with production-ready incident runbook sections (`SEV-1..3` SLAs, first-30-min checklist, escalation criteria, update templates, required postmortem schema).
- backlog update: marked `ACQ-REL-005` DONE in `plan/TASK_BACKLOG.md`; kept `ACQ-REL-003B`/`ACQ-REL-009..012` as TODO for remaining release-readiness execution.
- quality gates: pending in this automation pass.
- next: `ACQ-REL-009` performance SLO validation instrumentation.
- 2026-03-08T22:40:00+0100 - ACQ-REL-010A dashboard proposal-open funnel telemetry (UI) - Result: added shared web telemetry helper (`apps/web/src/telemetry.ts`) and instrumented dashboard proposal link clicks to emit `[telemetry:funnel]` events with `{ event: 'proposal_open', requestId }`. Added dashboard test coverage asserting telemetry emission on proposal-link click. Backlog maintenance: appended run-update split (`ACQ-REL-010` IN_PROGRESS, `ACQ-REL-010A` DONE, `ACQ-REL-010B` TODO). Quality gates: pending in this automation pass. Next: ACQ-REL-010B request-create + checkout funnel events.

## 2026-03-08T23:05:00+0100 - ACQ-REL-009A performance SLO baseline definition
- task: split `ACQ-REL-009` and complete baseline SLO-definition slice (`ACQ-REL-009A`).
- result:
  - added explicit API latency/error-budget and web vitals SLO targets in `docs/OPERATIONS.md`;
  - documented measurement method, breach policy, and evidence checklist for release readiness.
- backlog update:
  - moved `ACQ-REL-009` to `IN_PROGRESS`;
  - added `ACQ-REL-009A` DONE and `ACQ-REL-009B` TODO in `plan/TASK_BACKLOG.md`.
- quality gates: already green in this run (`lint`, `typecheck`, `test`, `build`).
- next: execute `ACQ-REL-009B` load sample and capture p95/web-vitals evidence.
- 2026-03-08T23:55:00+0100 - ACQ-REL-011A UAT report scaffold + backlog reconciliation - Result: published `docs/UAT_REPORT.md` with deterministic staging run metadata, entry criteria, scenario matrix, defect log, and sign-off schema; reconciled release backlog drift by marking `ACQ-REL-010` DONE (telemetry already shipped) and splitting `ACQ-REL-011` into `ACQ-REL-011A` DONE + `ACQ-REL-011B` TODO for staging execution evidence. Quality gates: pending in this automation pass. Next: execute `ACQ-REL-011B` in staging and populate real evidence.
- 2026-03-09T00:41:20+0100 - ACQ-REL-012 customer support/admin escalation baseline - Result: expanded `docs/OPERATIONS.md` with deterministic support contact paths, SLA targets, and dispute/escalation handling playbook including severity routing (`SEV-1..3`) and evidence requirements. Backlog maintenance: marked `ACQ-REL-012` DONE and added `ACQ-REL-013` TODO (UI support entrypoint on dashboard/operator detail). Quality gates: previously green in this run (`npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`). Next: ACQ-REL-013 UI support CTA + severity-routing hints.
- 2026-03-09T01:36:09+0100 - ACQ-REL-013 support severity-routing entrypoints (UI) - Result: added customer-visible support/escalation panel on dashboard and operator-facing support routing hints on request detail, including deterministic SEV-1/2/3 routing and contact aliases (`support`, `ops-escalation`, `legal-security`). Added UI coverage in `apps/web/tests/dashboard-page.test.tsx` and `apps/web/tests/operator-request-detail-page.test.tsx`. Backlog maintenance: marked `ACQ-REL-013` DONE and added backend follow-up `ACQ-REL-014` TODO for support ticket intake/audit parity.
- 2026-03-09T02:25:00+0100 - ACQ-REL-014 support-ticket intake API + immutable audit event (backend) - Result: added authenticated `POST /api/requests/:id/support-ticket` with strict severity/source/message validation, customer ownership guard, and immutable request-status-event audit persistence (`metadata.supportTicket`). Added integration-style route coverage for success + `401/403/400` branches in `apps/api/tests/requests-list.test.ts` and documented contract in `docs/API_SPEC.md`. Backlog maintenance: marked `ACQ-REL-014` DONE and added UI follow-up `ACQ-REL-015` TODO for in-page submit wiring.
- 2026-03-09T03:44:53+0100 - ACQ-REL-015 dashboard/operator support-ticket submit actions (UI) - Result: wired dashboard and operator request-detail support submission forms to `POST /api/requests/:id/support-ticket` with deterministic client-side validation (request selection + 10..1000-char message), API error-code mapping (`AUTH_REQUIRED`, `REQUEST_FORBIDDEN`, `VALIDATION_ERROR`), and explicit success/error copy. Added API client helper `submitSupportTicket` and expanded UI integration coverage in `apps/web/tests/dashboard-page.test.tsx` and `apps/web/tests/operator-request-detail-page.test.tsx`. Quality gates: `npm run lint` ✅, `npm run typecheck` ✅, `npm run test` ✅, `npm run build` ✅. Next: ACQ-REL-008C keyboard-only + contrast accessibility evidence pass.
- 2026-03-09T04:20:00+0100 - ACQ-REL-008C keyboard-only/contrast evidence + smoke gate hardening (UI) - Result: finalized accessibility release evidence by adding deterministic shell landmark assertions (primary/legal nav + main-content target) in `apps/web/tests/app-shell.test.tsx` and expanded `apps/web` smoke suite to include app-shell accessibility checks via `test:smoke`. Backlog maintenance: marked `ACQ-REL-008C` DONE and kept `ACQ-REL-011B` TODO as next release-readiness execution item requiring staging access. Quality gates: pending in this automation pass.
